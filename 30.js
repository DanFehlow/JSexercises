// Escreva um algoritmo para ler um valor (do teclado) e escrever (na tela) o seu
// antecessor.

let inputNumber = 5;
const results = Number(inputNumber - 1);

console.log(`Antes do número ${inputNumber} temos o número ${results}`);
